import * as types from "./action-types"
import {state} from "./default-state"
const reducer =(prevstate = state,actions)=>{
    let new_state = Object.assign({},prevstate);
        
}
export default reducer